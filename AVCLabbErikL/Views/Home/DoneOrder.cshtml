@model IEnumerable<OrderModel>
@{
    ViewData["Title"] = "DoneOrder";
}
<h1>DoneOrder</h1>
<h2>Thanks for you order! </h2>

@*<div class="d-flex flex-wrap">
    @foreach (var item in Model)
    {
        <form method="post">
            <div></div>
            <div class="card" style="margin:5px; box-shadow:2px;">
                <div class="card-body form-group" style="font-family: Arial, Helvetica, sans-serif">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item form-group" style="display:none" name="orderdate" value="@Html.DisplayFor(modelItem => item.OrderDate)">Order Date: @Html.DisplayFor(modelItem => item.OrderDate)</li>

                        <li class="list-group-item" name="orderid">Order ID: @item.Id</li>

                        <li class="list-group-item" name="total">Order Total: @item.OrderAmount</li>

                    </ul>

                </div>
            </div>
        </form>
    }
</div>*@

